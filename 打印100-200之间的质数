#include <stdio.h>

int main()
{
	int i = 0;
	for (i = 100; i <= 200; i++)
	{
		int m = 0;
		for (m = 2; m < i; m++)
		{
			if (i % m == 0)
			{
				break;
			}
		}
		if (i == m)
		{
			printf("%d ", i);
		}
	}
	return 0;
}
