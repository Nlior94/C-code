#include <stdio.h>

int main()
{
	int arr[10] = { 1,-1,2,-2,5,6,7,8,9,-4 };//10可以换成n
	int max = arr[0];
	int i = 0;
	for (i = 1; i < 10; i++)//i<10可换成i<n
	{
		if (arr[i] > max)
		{
			max = arr[i];
		}
	}
	printf("%d\n", max);
	return 0;
}
